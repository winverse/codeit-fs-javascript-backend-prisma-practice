export function assertSafeSeedTarget(
  _databaseUrl,
  _confirmation,
  _databaseName,
) {
  return false;
}

export async function seed(_prisma, _fixture) {
  // TODO: 안전 조건을 확인하고 관계 순서에 맞게 초기화·시딩하세요.
}
